import React from 'react';
import { StyledPlayerInfoBox } from '../../styles/profileStyles';

interface FixturesProps {}

export const Fixtures: React.FC<FixturesProps> = ({}) => {
	return <StyledPlayerInfoBox></StyledPlayerInfoBox>;
};
