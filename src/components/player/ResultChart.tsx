import React from 'react';
import { LargerInfoBox } from '../../styles/profileStyles';

interface ResultChartProps {}

export const ResultChart: React.FC<ResultChartProps> = ({}) => {
	return <LargerInfoBox></LargerInfoBox>;
};
